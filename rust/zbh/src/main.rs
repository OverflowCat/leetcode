fn main() {

    fn f(x: String) -> String {
        return 
    }

}

/*
let mut a = 1;
{
    let b = &mut a; // b 创建了一个 a 的可变引用
    *b += 1; // 修改值
    println!("{b}");
} // b 在这里 drop 了！引用又被归还了，现在 a 没有可变引用了
println!("{a}"); */